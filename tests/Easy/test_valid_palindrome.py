
import pytest
from LeetCode.src.Easy.valid_palindrome import func


@pytest.mark.parametrize(", expected", [])
def test_valid_palindrome(, expected):
    result = func()
    assert result == expected
